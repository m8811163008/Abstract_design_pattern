import 'package:flutter/material.dart';

/// An interface that defines the `render()`
/// method to render the activity indicator
/// component.
abstract class IActivityIndicator {
  Widget render();
}
